# R50 변경 내역

- 전체 메뉴의 보고서·자료에 읽기 전용 `데이터 무결성 점검` 추가
- 이력번호 형식·중복, 고아 기록, 미래 출생일, 번식·휴약 날짜, 일정 누락 검사
- Android 브리지가 없는 웹 미리보기에서 네이티브 시세 함수를 호출하던 오류 방지
- 내부 JavaScript 오류 문구를 사용자용 안내로 변환
- Android 파일 선택·저장·라이선스·시세 오류 메시지의 한글 깨짐 수정
- Android 앱/빌드 버전을 50.0.0(versionCode 50)으로 갱신
- GitHub Actions YAML 문법과 자동 검사 실행 방식 수정
- APK 서명 Secrets가 없어도 기존 업데이트 키 호환 기본값으로 빌드되도록 수정
- Java 소스 검사와 Node QA 실행 진입점 정리
- 개체 등록 시 출생일 누락·미래 날짜 저장을 차단하고 정상 날짜 저장 회귀 검사 추가
- 홈·환경설정의 시세 조회 실패에서 내부 예외 문구가 노출되지 않도록 처리
- 화면 제목의 접근성 이름 중복 제거
- YAML·CSS·JavaScript·HTML·Android XML·UTF-8·APK 서명키 구조 검사 추가
- 빌드 전 셸 문법 검사를 추가하고 Java 소스 목록 전달 방식을 안정화
- QA 패키지 보안 취약점 검사 결과 0건
- 모든 메뉴 제목을 한 줄 표시로 통일
- 홈 주요업무에 신규개체 등록과 송아지 집중관리 단독 메뉴 추가
- 체험기간 알림을 3일 이하에서만 표시하도록 변경
- 중요알림 본문 글자크기를 주요업무 메뉴 글자크기와 통일
- 홈 상단에 영문 제목과 로그아웃 버튼이 모바일에서도 표시되도록 변경
- 제목이 관리 업무인 화면에 개체 선택·수정편집 진입 버튼 추가
- 번식 자동관리 표에 개체별 번식기록 신규·수정편집 버튼 추가
- 번식 자동관리와 송아지 집중관리를 전문 집중관리에서 독립 화면으로 분리

검증 결과: Node 기반 자동 검사 289항목 통과, Android 파일 선택 단위검사 6항목 통과, 실제 브라우저에서 전체 메뉴 18개·무결성 점검·일정관리·보안설정·종료 확인창·번식 기록 수정편집·390px 모바일 화면을 직접 실행했으며 콘솔 오류·경고 0건을 확인.

## 2026-08-15 송아지·생산원가 제목 및 손글씨 입력 개선

- 모바일에서 `송아지 집중관리` 제목을 18px 한 줄 표시로 조정
- `송아지 육성기록 입력`, `개체별 생산원가 입력`을 16px 한 줄 표시로 조정
- 신규개체 등록의 손글씨 촬영 버튼 아래에 표준 항목명, 필기법, 촬영법, 검증 주의사항 추가
- 한글 필기체 인식이 어려울 때 휴대전화 키보드 마이크로 관리명을 입력하는 대안 안내
- Node 기반 자동 검사 15개 파일·298항목 전체 통과
- 390×844 실제 화면에서 세 제목 한 줄 표시와 손글씨 안내를 확인하고 콘솔 오류·경고 0건 확인

현재 작업 환경의 Android Studio JDK 21로 Java 검사를 통과했으며, APK 빌드에 필요한 Android SDK 플랫폼은 설치되어 있지 않아 APK 바이너리는 생성하지 않았습니다. `고객용 한우관리 APK 만들기`를 실행하면 서명된 R50 APK가 생성되며, 공동 저장소에서는 Actions secrets 등록을 권장합니다.
